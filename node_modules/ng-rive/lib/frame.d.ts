import { RiveCanvas as Rive } from "@rive-app/canvas-advanced";
import { Observable } from "rxjs";
export declare const nextFrame: (rive: Rive) => Promise<number>;
export declare const animationFrame: (rive: Rive) => Observable<number>;
